'use strict';
import {
    variables as _,
    Utils
} from './base';

export const Container = {
    wrapper: {
        flex: 1,
        flexDirection: 'column',
        flexWrap: 'wrap',
        position: 'relative',
    }
}
